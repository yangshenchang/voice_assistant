# Runtime dependencies for the voice assistant
# System deps: ffmpeg (needed by EdgeTTS MP3->WAV conversion), portaudio (PyAudio)

# --- Edge TTS (free TTS) ---
edge-tts>=6.1.0

# --- Speech-to-Text (Aliyun DashScope) + CosyVoice TTS ---
dashscope>=1.20.0

# --- Wake-word detection (local, sherpa-onnx) ---
sherpa-onnx>=1.10.0
pypinyin>=0.50.0
numpy>=1.26.0

# --- Audio I/O ---
PyAudio>=0.2.14

# --- LLM: 直连 OpenAI 兼容接口(/chat/completions), 已不需要 nanobot 网关的 websockets ---

# --- TTS / HTTP ---
httpx>=0.27.0
